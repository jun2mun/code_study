

# (1,2,3,,.....)
# 1전체 돌기 가능 , 전체 돌기 가능

def solution(food_times, k):
    sorted_food_times = sorted(food_times)
    iters = len(food_times) // k
    left = len(food_times) % k
    plus = 0



print(solution([3,1,2],5))